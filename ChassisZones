/*
Author : Patrick Ghazal
Created : Fri 6 July, 13:30:00
Last Modified :
*/

DECLARE @annee string = "2018";
DECLARE @mois string = "02";

//DECLARE @source string = "adl://gcpdatalakestore.azuredatalakestore.net/Solaris/";
DECLARE @source string = "SolarisInput/";
// 1ere etoile : mode, 2eme : jour, 3eme : zone (ou "sun" pour fich sun)
//DECLARE @dir string = "{*}/" + @annee + "/" + @mois + "/{*}/{*}/";
DECLARE @dir string = "{*}";

//DECLARE @outputdir string = "adl://gcpdatalakestore.azuredatalakestore.net/SolarisOutput/";

// Extracting original source data

@prstat =
	EXTRACT
		TimestampPr	string,
		Nproc		int,
		Swap		string,
		Rss 		string,
		Memory		string,
		Cpu			string,
		PrZone		string,
		Lwps		int,
		LoadAvg1	float,
		LoadAvg5	float,
		LoadAvg15	float
	FROM @source + "Prstat/" + @dir
    USING Extractors.Text(skipFirstNRows:1, delimiter:';');

@poolstat =
	EXTRACT
		TimestampPo	string,
		Id			int,
		pool		string,
		size		int,
		used		float,
		load		float
	FROM @source + "Poolstat/" + @dir
    USING Extractors.Text(skipFirstNRows:1, delimiter:';');

@sun =
    EXTRACT Host string,
            NomFrame string,
            SType string,
            SerNum string,
            TotChips int,
            TotCores int,
            TotThreads int,
            TotMem int,
            SZone string,
            CapMem int,
            PoolCpu string,
            ZoneCpuCap int,
            ZoneCpuShare int,
            ZoneMaxSwap float,
            ZoneMaxLkdMem int,
            ZoneMaxShmMem int,
            ZoneMaxLwps int
    FROM @source + "Sun/" + @dir
    USING Extractors.Text(skipFirstNRows:1, delimiter:',');
    //USING Extractors.Text(skipFirstNRows:1, delimiter:';');

@poolandpr =
	SELECT
		pr.*,
		pool.*
	FROM @prstat AS pr
	FULL OUTER JOIN
	@poolstat AS pool ON (pr.TimestampPr == pool.TimestampPo);

OUTPUT @poolandpr
TO "SolarisOutput/poolandpr.csv"
USING Outputters.Csv();

OUTPUT @sun
TO "SolarisOutput/useless_sun.csv"
USING Outputters.Csv();