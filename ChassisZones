/*
Author : Patrick Ghazal
Created : Fri 6 July, 13:30:00
Last Modified :
*/

DECLARE @annee string = "2018";
DECLARE @mois string = "02";

//DECLARE @source string = "adl://gcpdatalakestore.azuredatalakestore.net/Solaris/";
DECLARE @source string = "SolarisInput/";
// 1ere etoile : mode, 2eme : jour, 3eme : zone (ou "sun" pour fich sun)
//DECLARE @dir string = "{*}/" + @annee + "/" + @mois + "/{*}/{*}/";
DECLARE @dir string = "{*}";

//DECLARE @outputdir string = "adl://gcpdatalakestore.azuredatalakestore.net/SolarisOutput/";

// Extracting original source data

@prstat =
	EXTRACT
		TimestampPr	string,
		Nproc		int,
		Swap		string,
		Rss 		string,
		Memory		string,
		Cpu			string,
		PrZone		string,
		Lwps		int,
		LoadAvg1	float,
		LoadAvg5	float,
		LoadAvg15	float
	FROM @source + "Prstat/" + @dir
    USING Extractors.Text(skipFirstNRows:1, delimiter:';');

@poolstat =
	EXTRACT
		TimestampPo	string,
		Id			int,
		Pool		string,
		Size		int,
		Used		float,
		Load		float
	FROM @source + "Poolstat/" + @dir
    USING Extractors.Text(skipFirstNRows:1, delimiter:';');

@sun =
    EXTRACT Host string,
            NomFrame string,
            SType string,
            SerNum string,
            TotChips int,
            TotCores int,
            TotThreads int,
            TotMem int,
            SZone string,
            CapMem int,
            PoolCpu string,
            ZoneCpuCap int,
            ZoneCpuShare int,
            ZoneMaxSwap float,
            ZoneMaxLkdMem int,
            ZoneMaxShmMem int,
            ZoneMaxLwps int
    FROM @source + "Sun/" + @dir
    USING Extractors.Text(skipFirstNRows:1, delimiter:',');
    //USING Extractors.Text(skipFirstNRows:1, delimiter:';');

@prstat =
	SELECT
		TimestampPr.Substring(0, 10) AS Date,
		TimestampPr.Substring(11, 2).TrimEnd(':') AS Heure,
		TimestampPr,
		Nproc,
		Convert.ToInt32(Swap.TrimEnd('M')) AS SwapInt,
		Convert.ToInt32(Rss.TrimEnd('M')) AS RssInt,
		Convert.ToFloat(Memory.TrimEnd('%')) AS MemoryFloat,
		Convert.ToFloat(Cpu.TrimEnd('%')) AS CpuFloat,
		PrZone,
		Lwps,
		LoadAvg1,
		LoadAvg5,
		LoadAvg15
	FROM @prstat;

@poolstat =
	SELECT
		TimestampPo.Substring(0, 10) AS Date,
		TimestampPo.Substring(11, 2).TrimEnd(':') AS Heure,
		TimestampPo,
		Id,
		Pool,
		Size,
		Used,
		Load
	FROM @poolstat;

OUTPUT @poolstat
TO "SolarisOutput/poolstat.csv"
USING Outputters.Csv();

OUTPUT @sun
TO "SolarisOutput/sun.csv"
USING Outputters.Csv();

OUTPUT @prstat
TO "SolarisOutput/prstat.csv"
USING Outputters.Csv();